const loadContext = require('../../loadcontext');

it('newsService.yi2wan', async () => {
    const a = await loadContext();
    const newsService = a.beans.newsService;


    //
    process.exit(0);
}).timeout(100000);








